import type { Migration } from 'payload'

/**
 * Empty migration set.
 * Add real migrations later as { name, up, down } objects.
 */
export const migrations: Migration[] = []
export default migrations
